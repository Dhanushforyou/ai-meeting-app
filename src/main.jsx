import React from "react";
import ReactDOM from "react-dom/client";
import MeetingSummarizerApp from "./MeetingSummarizerApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MeetingSummarizerApp />
  </React.StrictMode>
);
